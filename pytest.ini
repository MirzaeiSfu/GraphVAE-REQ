[pytest]
testpaths = tests
markers =
    unit: no network, database, GPU, or real training
    postgres: requires GRAPHVAE_BO_TEST_STORAGE_URL and an isolated study name
    remote: requires explicitly approved remote worker slots
    gpu: invokes CUDA
    slow: invokes real bounded training/evaluation
